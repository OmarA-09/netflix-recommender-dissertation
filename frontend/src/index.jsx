import React from 'react';
import ReactDOM from "react-dom/client";
import './index.css';
import NetflixRecommenderApp from './NetflixRecommenderApp.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NetflixRecommenderApp />
  </React.StrictMode>
);